Update the existing Streamlit app and add a new page called "Integrations".

The app is currently a Creative Performance Analyzer for paid social creative data. Do not break the existing analyzer. Keep the core app focused on ranking creatives, calculating KPIs, and recommending which creatives to scale, cut, fix, or remix.

Add sidebar navigation with three pages:
1. Analyzer
2. Integrations
3. Methodology

Move the existing upload, KPI, ranking, charts, pattern analysis, and recommendations into the Analyzer page.

The new Integrations page should show how this tool could become a lightweight creative intelligence hub for a growth team.

Important:
This is a prototype integration layer, not a production auth system.
Do not store API keys permanently.
Do not hardcode API keys.
Do not print API keys in logs.
Do not commit API keys.
Use Streamlit session_state only for temporary prototype use.
Mask all API key inputs.
Add warning text that production versions should use OAuth, encrypted secret storage, environment variables, and role-based permissions.

Relevant integrations:
1. Meta Ads
2. TikTok Ads
3. Google Sheets
4. Airtable
5. Notion
6. Slack
7. Google Drive
8. Mixpanel
9. Amplitude
10. HubSpot

Remove Mailchimp. It is less relevant for this paid social creative workflow.

Page layout:
Create clean integration cards or sections for each tool.

Each integration card should include:
- Tool name
- Short description of why it matters
- API key or token input
- Optional account ID, workspace ID, base URL, or sheet URL field when relevant
- Test Connection button
- Status badge:
  - Not connected
  - Connected
  - Failed
  - Prototype only
- List of possible data this tool could pull

Integration descriptions:

Meta Ads:
Direct source for spend, impressions, clicks, CTR, conversions, creative IDs, platform-level performance, and cost per result.

TikTok Ads:
Direct source for short-form creative performance, thumbstop behavior, hold rates, CTR, spend, and conversion data.

Google Sheets:
Useful for live creative performance exports, media buyer notes, testing plans, and manual campaign trackers.

Airtable:
Useful for creative testing databases, hook libraries, concept tags, production status, test history, and learning logs.

Notion:
Useful for creative briefs, experiment documentation, weekly learnings, and team-facing summaries.

Slack:
Useful for sending scale, cut, and test-next recommendations directly to the growth team.

Google Drive:
Useful for linking creative assets, raw videos, thumbnails, briefs, and campaign exports.

Mixpanel:
Useful for product-side activation data, trial behavior, user cohorts, and trial-to-paid funnel analysis.

Amplitude:
Useful for product analytics, retention behavior, activation events, and user journey analysis.

HubSpot:
Useful for lead quality, lifecycle stage, CRM source attribution, and downstream conversion feedback.

Prototype behavior:
Only Google Sheets needs to perform a real data pull in this prototype.

For Google Sheets:
Allow the user to paste a public Google Sheets CSV export URL.
If provided, try to load it into pandas.
If successful:
- Show a preview of the data
- Show row and column count
- Allow the user to use this data in the Analyzer page
Store the loaded dataframe in Streamlit session_state so the Analyzer page can use it.

For all other integrations:
Create safe placeholder test functions.
The test should return "Connected" if the required field is non-empty.
Clearly label these as prototype connectors.
Add comments in the code showing where a real API request would be added later.

Example placeholder functions:
test_meta_ads_connection(access_token, ad_account_id)
test_tiktok_ads_connection(access_token, advertiser_id)
test_airtable_connection(api_key, base_id)
test_notion_connection(api_key)
test_slack_connection(bot_token)
test_google_drive_connection(api_key)
test_mixpanel_connection(api_secret)
test_amplitude_connection(api_key)
test_hubspot_connection(private_app_token)

Data pull options:
Create a section called "Possible Data Pulls".

For each integration, show possible future pulls.

Meta Ads:
- Creative-level spend
- CTR
- Impressions
- Clicks
- Conversions
- Cost per result
- Creative IDs
- Campaign and ad set metadata

TikTok Ads:
- Thumbstop rate
- Hold rate
- CTR
- Spend
- Trial starts
- Paid starts
- Video-level engagement

Google Sheets:
- Creative performance table
- Campaign export table
- Testing tracker
- Weekly reporting sheet

Airtable:
- Creative concept database
- Hook library
- Test status
- Creative owner
- Production stage
- Learning tags

Notion:
- Creative briefs
- Weekly growth notes
- Test summaries
- Experiment writeups

Slack:
- Scale and cut alerts
- Weekly creative summaries
- Test recommendations
- Team notifications

Google Drive:
- Creative files
- Video assets
- Thumbnails
- Briefs
- Exported reports

Mixpanel:
- Activation events
- Trial usage behavior
- Product engagement by cohort
- Trial-to-paid funnel events

Amplitude:
- User journey analysis
- Retention metrics
- Activation cohorts
- Funnel drop-off data

HubSpot:
- Lead source quality
- Lifecycle stage
- CRM conversion feedback
- Deal source attribution

Add a section called "Why these integrations matter".
Use this explanation:
"Manual CSV upload is useful for quick analysis, but growth teams usually make creative decisions from scattered data. Ad platforms show performance, Sheets and Airtable track test plans, Drive stores assets, Slack and Notion capture team learnings, and product analytics tools show whether paid users actually activate. This integration layer shows how the analyzer could become a lightweight creative intelligence hub instead of a one-off spreadsheet review."

Add a section called "Production improvements".
Include:
- OAuth instead of manual API keys
- Encrypted secret storage
- Environment variables for server-side secrets
- Role-based permissions
- Scheduled data refresh
- Source-specific data mapping
- API rate limit handling
- Error logging
- Secret scanning before deployment
- Clear separation between frontend inputs and backend data pulls

Methodology page:
Create a separate Methodology page that explains:
- KPI formulas
- Ranking logic
- Decision labels
- Why Cost per Paid Start matters most for this LoopNote case
- How the tool adapts to other campaign goals
- Why creative engagement metrics should be read alongside conversion metrics

KPI formulas to include:
Cost per paid start = Spend / Paid Starts
Cost per trial start = Spend / Trial Starts
Trial to paid conversion rate = Paid Starts / Trial Starts
Paid starts per $1,000 spend = Paid Starts / Spend * 1000
Trial starts per $1,000 spend = Trial Starts / Spend * 1000
CTR efficiency score = CTR / Cost per paid start
Thumbstop efficiency score = Thumbstop Rate / Cost per paid start
Hold efficiency score = 6s Hold Rate / Cost per paid start

Keep the design:
- Dark modern dashboard style
- Clean cards
- Simple badges
- Clear labels
- Demo-friendly layout
- No unnecessary complexity

Make sure the app still runs with:
streamlit run app.py

Also update requirements.txt if needed.